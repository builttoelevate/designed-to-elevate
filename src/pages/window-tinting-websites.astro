---
// /window-tinting-websites — dedicated Google Ads landing page for WINDOW TINT
// SHOP OWNERS (not their end customers). No nav, single conversion path.
// Minimal header + footer baked in here so we don't reuse SiteHeader/SiteFooter
// (which would leak paid traffic off to other pages). Mirrors the
// auto-detailing-websites + barbershop-websites LPs. noindex so it doesn't
// compete with the indexed /window-tint-websites SEO page. LandingLayout
// provides the global <head> (meta, OG/Twitter, canonical), the sticky-mobile-
// CTA observer JS, and the call/text dialog.
import LandingLayout from '../layouts/LandingLayout.astro';
import BreadcrumbSchema from '../components/BreadcrumbSchema.astro';
import QuoteFlowSection from '../components/landing/QuoteFlowSection.astro';

const SITE = 'https://designedtoelevate.co';
const PHONE_DISPLAY = '(740) 617-6488';
const PHONE_TEL = 'tel:7406176488';
const EMAIL = 'bilsonxnc@gmail.com';
const QUOTE_HREF = '#quote';

// Service schema for a web design studio that builds tint-shop websites.
const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: 'Window Tint Shop Website Design',
  serviceType: 'Website and quote-system design for window tint shops',
  description:
    'Window tint shop website builder for auto, residential, and commercial tint. Instant auto-tint quote flows, in-home residential estimates, commercial site assessments, lead capture, and a text-to-book handoff. Two product lines: add the wizard to your existing site (Form line, $997–$2,497 install) or build a new site with the wizard included (Website line, $1,497–$4,497 install). Install pricing only. You own everything at handoff — no required ongoing fee.',
  provider: { '@id': `${SITE}/#organization` },
  areaServed: { '@type': 'State', name: 'Ohio' },
  audience: { '@type': 'BusinessAudience', name: 'Window tint shops (auto, residential, commercial)' },
  url: `${SITE}/window-tinting-websites`,
  offers: {
    '@type': 'AggregateOffer',
    lowPrice: '997',
    highPrice: '4497',
    priceCurrency: 'USD',
    offerCount: '6',
    description: 'Form line install: $997 / $1,497 / $2,497. Website line install: $1,497 / $2,497 / $4,497. You own everything outright at handoff — no required ongoing fee.',
  },
};

const painCards = [
  {
    title: "One generic form can't sell three different customers",
    body: 'Your auto, residential, and commercial buyers don’t shop the same way. A single "contact us" box treats them like they do — and loses all three.',
  },
  {
    title: 'Auto-tint buyers want a price now, not a callback',
    body: "Tint is a fast, transactional buy. If your site can't price a two-front carbon job on a Civic in two minutes, they're already tapping the next shop's Google listing.",
  },
  {
    title: 'Home and commercial leads need a different close',
    body: 'A homeowner with a sun-bleached floor and a property manager with a glare problem don’t want an instant price. They want an estimate booked. A flat form books neither.',
  },
  {
    title: 'Every lead that doesn’t call is gone',
    body: 'No name, no vehicle, no scope, no follow-up. A form that only fires when someone finishes is a form that loses everyone who doesn’t.',
  },
];

// Outcome-first deliverables — outcome owners care about, then the feature underneath.
const whatYouGet = [
  {
    outcome: 'Auto-tint buyers see a real price in two minutes — not a callback request.',
    feature: 'Instant-quote flow priced across 17 vehicle body-class buckets — coupe to crew-cab truck.',
  },
  {
    outcome: 'Home and commercial leads close on a booked estimate, not a guess.',
    feature: 'Scenario-led pages route each segment to its own CTA — in-home estimate or site assessment.',
  },
  {
    outcome: 'The ceramic upgrade sells itself.',
    feature: 'Carbon, Nano Ceramic, and Ceramic IR shown side-by-side with the upgrade delta inline — "only +$X from Carbon."',
  },
  {
    outcome: 'The undecided customer still leaves with a price.',
    feature: 'Per-zone darkness/VLT picker plus a "recommend for me" branch so nobody bounces on a question.',
  },
  {
    outcome: 'Even abandons still land in your dashboard.',
    feature: 'Partial-lead capture grabs name + phone a step before the final screen — so you can follow up.',
  },
  {
    outcome: 'The finished quote texts itself to you to book.',
    feature: 'Quote ID, vehicle, film, coverage, darkness — pre-filled in a text to your phone, or routed to your booking link.',
  },
  {
    outcome: 'You rank for "window tint [town]," not just your shop name.',
    feature: 'Local SEO structure, service-area schema, and Google Business Profile alignment built into the build.',
  },
  {
    outcome: 'You own it outright — no required monthly, cancel anytime, site keeps running.',
    feature: 'Code, repo, and domain in your name at handoff. Hosting tier is free for shops your size.',
  },
];

// Old way vs new way — explicit two-column contrast.
const oldWay = [
  'One generic form for three different buyers',
  'Auto-tint customers wait for a callback that loses to the next shop',
  'Home and commercial leads get the same "we\'ll get back to you"',
  'Loses every visitor who wanted a price or a booked estimate',
];

const newWay = [
  'Three segment-specific flows on one site',
  'Auto tint priced instantly by vehicle and film tier',
  'Residential + commercial routed to a booked estimate',
  'Captures the lead before they bounce — every segment',
];

// What happens after the click — the invisible system, made tangible.
const afterClickSteps = [
  { num: '01', label: 'Google ad click',          body: '"Window tint [town]," "ceramic tint near me," or your shop name' },
  { num: '02', label: 'Segment-specific page',    body: 'Auto, residential, or commercial — ad params drop them in the right flow' },
  { num: '03', label: 'Pick vehicle + film tier', body: 'For auto: vehicle, coverage, film tier, darkness. For home/commercial: scenario.' },
  { num: '04', label: 'See a real ballpark',      body: 'Auto gets an instant price. Home/commercial gets a booked estimate slot.' },
  { num: '05', label: 'Lead in your dashboard',   body: 'Quote texted to your phone, contact captured even if they bail' },
];

// Pricing tiers — two-line structure pulled from PRICING.md → PUBLIC section.
// Form line = add the wizard to an existing site. Website line = build the site too.
// Install price only. Care plans are INTERNAL (sold on the call, never rendered here)
// — see PRICING.md → INTERNAL section for the $97/$197/$297 structure and sales script.
const formLineTiers = [
  {
    id: 'form_t1',
    name: 'Quote',
    code: 'T1',
    setup: '$997',
    blurb: 'Customer gets a real price, the lead hits your dashboard, you schedule it manually.',
    features: [
      'Guided quote wizard with automatic pricing',
      'Final quote → pre-filled text to the shop',
      'Lead lands in the dashboard + calendar',
      'Customer + shop both get a quote email',
    ],
    highlight: false,
  },
  {
    id: 'form_t2',
    name: 'Quote + Reviews',
    code: 'T2',
    setup: '$1,497',
    blurb: 'Everything in T1, plus auto-texts each customer a Google-review request after the job (skips no-shows).',
    features: [
      'Everything in T1',
      'Automated SMS Google-review requests',
      'Review request auto-skips no-shows',
    ],
    highlight: true,
    tag: 'Most form-line builds',
  },
  {
    id: 'form_t3',
    name: 'Quote + Booking',
    code: 'T3',
    setup: '$2,497',
    blurb: 'Everything in T2, plus the customer books their own slot online — runs on autopilot.',
    features: [
      'Everything in T2',
      'Customer self-service booking → calendar',
    ],
    highlight: false,
  },
];

const websiteLineTiers = [
  {
    id: 'web_t1',
    name: 'Starter Site',
    code: 'T1',
    setup: '$1,497',
    blurb: 'Fast, modern website + smart lead-capture form. The foundation — easy to upgrade to the full system later.',
    features: [
      'Custom website (mobile-first, fast, owned)',
      'Local SEO structure + analytics',
      'Smart lead-capture form (1–2 questions, routed to text/email)',
    ],
    highlight: false,
  },
  {
    id: 'web_t2',
    name: 'Quote Site',
    code: 'T2',
    setup: '$2,497',
    blurb: 'Everything in T1, plus the full Quote wizard built into the site (Form-line T1 system).',
    features: [
      'Everything in T1',
      'Full guided quote wizard + automatic pricing',
      'Quote → text-to-shop handoff',
      'Lead dashboard + calendar + customer/shop emails',
    ],
    highlight: true,
    tag: 'Most website builds',
  },
  {
    id: 'web_t3',
    name: 'The Works',
    code: 'T3',
    setup: '$4,497',
    blurb: 'Everything Fitchin has — T2 + automated SMS reviews + customer self-service online booking. The whole system, on autopilot.',
    features: [
      'Everything in T2',
      'Automated SMS Google-review requests (skips no-shows)',
      'Customer self-service online booking → calendar',
    ],
    highlight: false,
  },
];

const segments = [
  {
    tag: 'Transactional',
    title: 'Auto tint: price it in two minutes',
    body: 'Instant-quote flow. The customer picks their vehicle, film tier, coverage, and darkness — and sees a real price before they ever call. Built to catch the buyer who’s deciding right now.',
    cta: 'Get My Quote → 2 Min',
  },
  {
    tag: 'Consultative',
    title: 'Residential tint: book the estimate',
    body: 'Scenario-led pages name the problem — the bleached line on the hardwood, the bedroom that hits 85° by noon — then route the homeowner to a booked in-home estimate instead of a price they won’t trust sight-unseen.',
    cta: 'Schedule In-Home Estimate',
  },
  {
    tag: 'Problem-first',
    title: 'Commercial tint: solve, don’t sell tiers',
    body: 'Property managers shop by what they’re fixing — heat, security, privacy, glare. The site organizes by problem, handles after-hours scheduling and certificate-of-insurance requests, and routes to a site assessment.',
    cta: 'Schedule Site Assessment',
  },
];

const caseFacts = [
  'Live in production at Fitchin',
  '17 vehicle body-class buckets for accurate pricing',
  'Carbon, Nano Ceramic & Ceramic IR compared inline',
  'Per-zone darkness with a recommend-for-me branch',
  'Partial-lead capture — no quote lost',
];

const caseBullets = [
  'Instant auto-tint pricing across 17 body-class buckets — coupe to crew-cab truck',
  'Film tiers shown inline so customers see the value gap (Carbon → Nano Ceramic → Ceramic IR)',
  'Per-zone darkness/VLT selection, plus a "recommend for me" branch for the undecided',
  'Sun strips, full windshield, roof, and old-tint removal priced automatically',
  'Lead capture before the final step — partial quotes still land in the dashboard',
  'The finished quote deep-links into a text to book — no phone tag',
];

const proofItems = [
  {
    src: '/wizard/step-2-tint.png',
    title: 'Tint exactly what they want',
    description:
      'Full vehicle, two fronts, windshield, or a sun strip — the customer picks the coverage, and the price follows. No "call for details."',
  },
  {
    src: '/wizard/step-5-summary.png',
    title: 'An estimate they can adjust',
    description:
      'Customers see an itemized quote they can toggle line by line, with a live subtotal, tax, and total — before anything is booked.',
  },
  {
    src: '/wizard/step-6-final-quote.png',
    title: 'Priced and ready to book',
    description:
      'A real quote with a number, then one tap to text it to the shop or book online. The lead is already captured either way.',
  },
];

const signatureFeatures = [
  {
    title: 'Vehicle-accurate pricing, automatically',
    body: 'Behind the scenes, the engine maps every vehicle to one of 17 body-class buckets — coupe, compact SUV, 3-row, crew-cab truck, minivan. A Wrangler and a sedan get priced right, without you touching a thing.',
  },
  {
    title: 'Film tiers that upsell themselves',
    body: 'Carbon, Nano Ceramic, and Ceramic IR shown side by side, with the upgrade delta right there — "only +$X from Carbon." Customers talk themselves into the better film. Your average ticket climbs without a sales pitch.',
  },
  {
    title: 'The ad knows where to land',
    body: 'Running a Google Ad for ceramic tint? The link drops the customer straight into the right flow with a service parameter — no menu, no friction. Every campaign points at the exact quote it promised.',
  },
];

const howItWorks = [
  {
    label: 'Fast on a phone, ad-ready',
    value:
      'Built on Astro and deployed on Vercel — sub-second mobile loads, which keeps Google Ads Quality Score high and stops paid clicks from bouncing.',
  },
  {
    label: 'Your booking and payments stay yours',
    value:
      'Your scheduler and POS keep running. The quote hands off to a text or your booking link, with an optional Stripe deposit and an add-to-calendar confirmation to hold the slot.',
  },
  {
    label: 'Every quote in one dashboard',
    value:
      'Finished or abandoned, each quote lands with the vehicle, film, coverage, darkness, and add-ons the customer picked — so follow-up isn’t a phone-tag exercise.',
  },
  {
    label: 'Tracking baked in',
    value:
      'Funnel analytics on every step, tagged by service, show where each segment drops off and which ads bring booked work — so you scale what converts and kill what doesn’t.',
  },
];

const whoFor = [
  'You run auto tint, residential, commercial — or any two of the three',
  'Your pricing changes by vehicle, film tier, coverage, and darkness',
  'You’re running — or about to run — Google Ads or Google Business Profile clicks',
  'You’re tired of one generic form trying to sell three different buyers',
  'You want the lead captured even when the customer doesn’t call',
];

const faqs = [
  {
    q: 'How much does a window tint shop website cost?',
    a: "Two product lines, six tiers, install-only pricing. If you already have a website you're keeping, we add just the wizard — Form line, $997 (Quote) / $1,497 (Quote + Reviews) / $2,497 (Quote + Booking) install. If you need a new website too, we build the system in — Website line, $1,497 (Starter Site) / $2,497 (Quote Site) / $4,497 (The Works) install. No required monthly on any tier. You own everything at handoff — the code, the repo, the domain. Cancel anytime and the site freezes as-is and keeps working.",
  },
  {
    q: 'Do I need a new website, or can you just add the wizard to mine?',
    a: "Both — your call. If your current tint-shop site is fine and you just want the quoting + lead-capture system on top of it, that's the Form line: $997 (T1 Quote) / $1,497 (T2 Quote + Reviews) / $2,497 (T3 Quote + Booking) install. If you need a new site too — or your current one is a Wix/Squarespace you're trying to leave — that's the Website line: $1,497 (Starter Site) / $2,497 (Quote Site) / $4,497 (The Works) install. Same wizard either way. You own everything at handoff either way.",
  },
  {
    q: 'Is there a monthly fee?',
    a: "No required monthly. You own your site outright — the code, the repo, the domain. The site keeps running whether you pay us anything ongoing or not. Optional care plans are available if you'd rather not manage hosting and maintenance yourself — we'll cover that on the call after we figure out which tier fits your shop.",
  },
  {
    q: 'Can one site really handle auto, residential, and commercial tint?',
    a: "Yes — that's the whole point. Each segment gets its own flow and call-to-action on the same site. Auto tint gets instant pricing; residential and commercial route to booked estimates. One site, three buyers, three ways to close.",
  },
  {
    q: 'How long does it take?',
    a: 'About 3–5 weeks from kickoff to launch on Website tiers; 1–3 weeks on Form line. Depends on how many segments you run (auto / residential / commercial) and how deep the pricing logic goes.',
  },
  {
    q: 'Can I start with the wizard and upgrade to a full website later?',
    a: "Yes. If you start on the Form line and later want a full website, your original install credits against the new Website tier — so a Form T2 client ($1,497) upgrading to Website T2 ($2,497) pays the $1,000 difference, not a fresh build cost. The wizard and dashboard you already own carry into the new build; we don't rebuild from scratch.",
  },
  {
    q: 'Do I have to change how I take bookings or payments?',
    a: 'No. Whatever you use to schedule and take payment keeps running. The finished quote hands off to a pre-filled text or your online booking link, with an optional Stripe deposit if you want to hold the slot.',
  },
  {
    q: 'Can you match my exact film tiers and pricing?',
    a: 'Yes. The engine is built around your real pricing — by vehicle, film tier, coverage, darkness, and add-on. When your prices change, you update them yourself and the site reflects it on the next load — no redeploy, no waiting on me.',
  },
  {
    q: 'Can I update prices later?',
    a: "Yes. Pricing is editable through a simple admin — no code, no developer needed. Change a base rate, swap a film tier, add a new vehicle class or add-on, save, and the live site reflects it on the next load.",
  },
  {
    q: 'What if I already have a website?',
    a: "Most shops do. Your existing site can either be replaced cleanly at launch or kept running while this lives as a dedicated landing page for paid traffic — your call. Either way, the old site keeps running until handoff so there's no downtime.",
  },
  {
    q: 'Do I need a new logo or photos?',
    a: "No. We work with what you have. If your logo or photos would limit conversion, we'll flag it and either tackle it together or leave it to you — nothing about the build is dependent on a rebrand. We can drop in better photography over time once leads start flowing.",
  },
  {
    q: 'Will this help with Google Ads and local SEO?',
    a: "Yes on both. A faster, message-matched landing page raises your Google Ads Quality Score, which lowers cost per click — and the quote flow gives Ads a clean conversion event to optimize against. Local SEO structure (service-area pages, schema, Google Business Profile alignment) is baked in so you can rank for \"window tint [town]\" searches, not just your exact shop name.",
  },
  {
    q: 'How is this different from Wix, Squarespace, or WordPress?',
    a: "Wix and Squarespace rent you a locked template — you pay forever, and the second you stop, the site goes dark. WordPress is yours but stacks its own ongoing costs (hosting, plugins, security, theme updates). This is custom-built around how your tint shop quotes and books, the code is yours at handoff, and nothing gets shut off if you walk away. You own it outright.",
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};
---
<LandingLayout
  title="Window Tint Shop Websites That Turn Clicks Into Booked Jobs"
  description="Window tint shop websites with quote systems — instant auto-tint pricing, in-home residential estimates, and commercial site assessments. Book a free call."
  pathname="window-tinting-websites"
  noindex={true}
>
  <script type="application/ld+json" set:html={JSON.stringify(serviceSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <BreadcrumbSchema items={[
    { name: 'Home', url: `${SITE}/` },
    { name: 'Window Tint Shop Websites', url: `${SITE}/window-tinting-websites` },
  ]} />

  <main>
    <!-- FIXED HERO BACKGROUND — pinned to the viewport with position:fixed so it
         stays still while content scrolls over it (works on iOS Safari, which
         ignores background-attachment:fixed). Sits at z-0; the hero below is
         transparent to reveal it, and everything after the hero is wrapped in an
         opaque bg-bg layer that scrolls over the top of it. -->
    <div class="fixed inset-0 z-0 pointer-events-none" aria-hidden="true">
      <img
        src="/window-tint/tint-shop-hero.jpg"
        alt=""
        class="absolute inset-0 h-full w-full object-cover"
        width="1672"
        height="941"
        loading="eager"
        decoding="async"
        fetchpriority="high"
      />
      <div class="absolute inset-0 bg-bg/45"></div>
      <div class="absolute inset-0 bg-gradient-to-r from-bg/75 via-bg/32 to-bg/12"></div>
      <div class="absolute inset-0 bg-gradient-to-b from-bg/15 via-transparent to-bg"></div>
    </div>

    <!-- HERO -->
    <section id="top" class="relative z-10 overflow-hidden border-b border-line">

      <!-- MINIMAL LANDING HEADER — no nav, transparent over the hero. -->
      <header class="relative z-30 w-full">
        <div class="mx-auto max-w-7xl px-5 sm:px-8 py-4 sm:py-5 flex items-center justify-between gap-3 sm:gap-4">
          <a href="#top" class="flex items-center" aria-label="Designed to Elevate">
            <img
              src="/logo-wordmark-orange.png"
              alt="Designed to Elevate — Ohio web design studio for service businesses"
              width="320"
              height="96"
              class="h-8 sm:h-10 w-auto"
            />
          </a>
          <div class="flex items-center gap-2 sm:gap-3">
            <a
              href={PHONE_TEL}
              class="inline-flex items-center gap-1.5 sm:gap-2 font-display font-medium text-[0.78rem] sm:text-[0.85rem] text-text-soft hover:text-accent transition-colors px-2 py-2 rounded-md"
              aria-label={`Call or text Bill at ${PHONE_DISPLAY}`}
            >
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92Z"/>
              </svg>
              <span class="hidden xs:inline sm:inline">{PHONE_DISPLAY}</span>
              <span class="xs:hidden sm:hidden">Call</span>
            </a>
            <a href={QUOTE_HREF} class="btn-primary !py-2.5 !px-3.5 sm:!px-4 !text-[0.78rem] sm:!text-[0.85rem] whitespace-nowrap">
              Get My Website Estimate
              <span aria-hidden="true">↗</span>
            </a>
          </div>
        </div>
      </header>

      <!-- HERO CONTENT -->
      <div class="relative z-10 mx-auto max-w-6xl px-5 sm:px-8 pt-6 sm:pt-10 pb-14 sm:pb-20">
        <div class="grid lg:grid-cols-[1.1fr_0.9fr] gap-10 lg:gap-14 items-center">
          <div>
            <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Window Tint Shop Websites</span>
            <h1 class="mt-5 text-[2.2rem] sm:text-[2.9rem] lg:text-[3.3rem] leading-[1.06] font-bold tracking-tight text-balance">
              Get a <span class="text-accent">tint quote</span> before they call another shop.
            </h1>
            <p class="mt-6 text-text-soft text-[1.05rem] sm:text-[1.1rem] leading-relaxed max-w-xl">
              A mobile-first website and quote system for tint shops — visitors become ready-to-book leads sent straight to your phone. You own the code outright.
            </p>

            <!-- HERO PROOF ELEMENT — live-in-production badge + named shop. -->
            <div class="mt-5 inline-flex items-center gap-2.5 rounded-full border border-line bg-surface-1/60 px-3 py-1.5">
              <span class="relative flex h-1.5 w-1.5" aria-hidden="true">
                <span class="absolute inline-flex h-1.5 w-1.5 rounded-full bg-accent/60 animate-ping"></span>
                <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-accent"></span>
              </span>
              <span class="font-display text-[0.75rem] uppercase tracking-[0.14em] text-text-soft">Live in Production · Fitchin Tint &amp; Detail</span>
            </div>

            <div class="mt-7 flex flex-wrap gap-3">
              <a href={QUOTE_HREF} data-hero-cta class="btn-primary">
                Get My Website Estimate
                <span class="text-[0.85em] font-normal opacity-80">→ 60 sec</span>
              </a>
              <a href="#segments" class="btn-ghost">
                How we build by segment
                <span aria-hidden="true">→</span>
              </a>
            </div>

            <div class="mt-3 text-[0.85rem] text-text-muted">
              No spam. No pressure. I'll reply within 24 hours.
            </div>
          </div>

          {/* Hero product shot — art-directed per device (only the matching image
              downloads). Desktop (>=1024px): landscape laptop shot of the live
              Fitchin site. Mobile/tablet: the two-phone composite, full-bleed +
              flush to the hero's bottom edge on phones. (Shared images for now —
              tint-specific shots can be swapped in later.) */}
          <div class="relative -mx-5 -mb-14 sm:mx-0 sm:mb-0">
            <div class="hidden sm:block absolute -inset-6 bg-gradient-to-br from-accent/30 via-accent/5 to-transparent rounded-[2rem] blur-3xl opacity-60" aria-hidden="true"></div>
            <picture>
              <source media="(min-width: 1024px)" srcset="/showcase/auto-detail-hero-desktop.webp" width="1100" height="825" />
              <img
                src="/showcase/auto-detail-hero.jpg"
                alt="The Fitchin website and its built-in quote wizard — a real production build by Designed to Elevate."
                class="relative block w-full h-auto sm:max-w-[24rem] sm:mx-auto lg:max-w-none sm:rounded-xl"
                width="900"
                height="1125"
                loading="eager"
                decoding="async"
              />
            </picture>
          </div>
        </div>
      </div>
    </section>

    <!-- CONTENT OVER THE FIXED BACKGROUND — opaque bg-bg so the hero photo only
         shows behind the hero, then this layer scrolls up over it. -->
    <div class="relative z-10 bg-bg">

    <!-- PAIN -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> The Problem</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.5rem] leading-[1.07] font-bold">
            Most tint shops run one website for three completely different buyers.
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            It’s not a hustle problem. It’s a system problem. A single contact form can’t price an auto job, book a home estimate, and scope a commercial install — so it does none of them well.
          </p>
        </div>

        <div class="mt-12 grid sm:grid-cols-2 gap-5">
          {painCards.map((c) => (
            <div class="card p-7 flex flex-col gap-3">
              <h3 class="font-display font-semibold text-[1.05rem] leading-snug text-text">{c.title}</h3>
              <p class="text-[0.95rem] text-text-soft leading-relaxed">{c.body}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- OLD WAY vs NEW WAY — contrast block. Justifies costing more than a $500 template. -->
    <section class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Old vs. New</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.5rem] leading-[1.07] font-bold">
            A normal tint website vs. a real quote system.
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            Both cost money to run. Only one of them prices the job and captures the lead.
          </p>
        </div>

        <div class="mt-12 grid md:grid-cols-2 gap-5">
          <div class="card p-7 sm:p-8 flex flex-col">
            <div class="font-display text-[0.74rem] uppercase tracking-[0.18em] text-text-muted mb-4">A normal tint website</div>
            <ul class="space-y-3 flex-1">
              {oldWay.map((item) => (
                <li class="flex items-start gap-3 text-[0.98rem] text-text-soft leading-snug">
                  <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-text-muted/15 text-text-muted flex-shrink-0" aria-hidden="true">
                    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                  </span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>

          <div class="card p-7 sm:p-8 flex flex-col border-accent/40 bg-accent/[0.04]">
            <div class="font-display text-[0.74rem] uppercase tracking-[0.18em] text-accent mb-4">A Designed to Elevate quote system</div>
            <ul class="space-y-3 flex-1">
              {newWay.map((item) => (
                <li class="flex items-start gap-3 text-[0.98rem] text-text leading-snug">
                  <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-accent/20 text-accent flex-shrink-0" aria-hidden="true">
                    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                  </span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- WHAT HAPPENS AFTER THE CLICK — the invisible system, made tangible. -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> The Flow</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            What happens after the click?
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            Most websites stop at the click. We build the path after it — from paid traffic to a quote you can book.
          </p>
        </div>

        <ol class="mt-12 grid sm:grid-cols-2 md:grid-cols-5 gap-4 md:gap-3">
          {afterClickSteps.map((s) => (
            <li class="card p-5 flex flex-col">
              <div class="font-display text-accent text-[0.74rem] uppercase tracking-[0.2em] mb-2">{s.num}</div>
              <h3 class="font-display font-semibold text-[1rem] text-text leading-snug">{s.label}</h3>
              <p class="mt-2 text-[0.88rem] text-text-soft leading-relaxed">{s.body}</p>
            </li>
          ))}
        </ol>

        <div class="mt-12 flex justify-center">
          <a href={QUOTE_HREF} class="btn-primary">
            Get My Website Estimate
            <span class="text-[0.85em] font-normal opacity-80">→ 60 sec</span>
          </a>
        </div>
      </div>
    </section>

    <!-- WHAT YOU GET — outcomes first, feature underneath. -->
    <section class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-5xl px-5 sm:px-8 py-20 sm:py-24">
        <span class="eyebrow"><span class="dot" aria-hidden="true"></span> What You Get</span>
        <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
          Not a prettier homepage. A booking machine.
        </h2>
        <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed max-w-3xl">
          What changes for your shop, and what's running under the hood to make it happen.
        </p>

        <ul class="mt-10 grid sm:grid-cols-2 gap-x-8 gap-y-7">
          {whatYouGet.map((item) => (
            <li class="flex items-start gap-3">
              <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <div class="min-w-0">
                <div class="font-display font-semibold text-[1rem] text-text leading-snug">{item.outcome}</div>
                <div class="mt-1.5 text-[0.92rem] text-text-soft leading-relaxed">{item.feature}</div>
              </div>
            </li>
          ))}
        </ul>
      </div>
    </section>

    <!-- THREE-PATTERN ARCHITECTURE (centerpiece differentiator) -->
    <section id="segments" class="relative border-b border-line">
      <div class="grid-bg absolute inset-0 z-0 opacity-40" aria-hidden="true"></div>
      <div class="relative z-10 mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-28">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Built by Segment</span>
          <h2 class="mt-5 text-[2rem] sm:text-[2.5rem] leading-[1.05] font-bold">
            We build sites that match how each tint customer actually buys.
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            Auto, residential, and commercial are three different sales. Each one gets the flow, the pricing logic, and the call-to-action that closes it — on the same site.
          </p>
        </div>

        <div class="mt-12 grid md:grid-cols-3 gap-6">
          {segments.map((s) => (
            <article class="card card-hover p-7 flex flex-col gap-4">
              <span class="inline-flex w-fit items-center rounded-full border border-line px-2.5 py-1 font-display text-[0.62rem] font-bold uppercase tracking-[0.16em] text-accent">
                {s.tag}
              </span>
              <h3 class="font-display font-semibold text-[1.2rem] leading-snug text-text">{s.title}</h3>
              <p class="text-[0.95rem] text-text-soft leading-relaxed flex-1">{s.body}</p>
              <div class="pt-3 border-t border-line">
                <div class="font-display text-[0.68rem] uppercase tracking-[0.16em] text-text-muted mb-2">The CTA we build for it</div>
                <span class="inline-flex items-center gap-1.5 rounded-md bg-accent/12 px-3 py-1.5 font-display font-semibold text-[0.85rem] text-accent">
                  {s.cta}
                </span>
              </div>
            </article>
          ))}
        </div>

        <figure class="mt-14">
          <div class="relative">
            <div class="absolute -inset-6 bg-gradient-to-br from-accent/20 via-accent/5 to-transparent rounded-[2rem] blur-3xl opacity-50" aria-hidden="true"></div>
            <div class="relative card overflow-hidden">
              <img
                src="/home/features/fitchin-guided-tint.png"
                alt="Guided Tint Selection screen from the live Fitchin auto-tint flow, comparing Carbon, Nano Ceramic, and Ceramic IR film tiers side by side with prices and an upgrade callout."
                class="block w-full h-auto"
                width="1448"
                height="1086"
                loading="lazy"
                decoding="async"
              />
            </div>
          </div>
          <figcaption class="mt-4 text-[0.92rem] text-text-muted text-center max-w-2xl mx-auto">
            The auto-tint instant-quote flow, live at Fitchin: film tiers compared side by side, priced by vehicle.
          </figcaption>
        </figure>
      </div>
    </section>

    <!-- FEATURED BUILD (Fitchin) — #case-study is a Google Ads sitelink target. -->
    <section id="case-study" class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-7xl px-5 sm:px-8 py-20 sm:py-28">
        <div class="max-w-3xl mb-12">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Featured Build</span>
          <h2 class="mt-5 text-[2rem] sm:text-[2.5rem] leading-[1.05] font-bold">
            Fitchin Automotive Detailing &amp; Window Tinting
          </h2>
          <div class="mt-3 text-text-muted text-[0.95rem]">Coshocton, Ohio</div>
        </div>

        <div class="grid lg:grid-cols-[0.9fr_1fr] gap-10 lg:gap-14 items-center">
          <div class="relative">
            <div class="absolute -inset-6 bg-gradient-to-br from-accent/30 via-accent/5 to-transparent rounded-[2rem] blur-3xl opacity-50" aria-hidden="true"></div>
            <article class="relative card p-7 sm:p-8">
              <span class="inline-flex items-center gap-2 rounded-full bg-accent px-2.5 py-1 font-display text-[0.62rem] font-bold uppercase tracking-[0.16em] text-bg">
                <span class="relative flex h-1.5 w-1.5" aria-hidden="true">
                  <span class="absolute inline-flex h-1.5 w-1.5 rounded-full bg-bg/60 animate-ping"></span>
                  <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-bg"></span>
                </span>
                Live in Production
              </span>
              <ul class="mt-6 space-y-4">
                {caseFacts.map((f) => (
                  <li class="flex items-start gap-3 text-[1rem] text-text leading-snug">
                    <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                      <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                    </span>
                    <span>{f}</span>
                  </li>
                ))}
              </ul>
            </article>
          </div>

          <div>
            <p class="text-text-soft text-[1.05rem] leading-relaxed">
              Fitchin was the proving ground for this system. Eric runs a real auto detailing, ceramic, and window tinting shop in Coshocton, Ohio — with real pricing complexity across three segments and real customers who need clear answers before they book. The old site could describe the films, but it still couldn't price the work. The rebuild replaced the basic contact-form experience with a guided tint wizard that handles every vehicle, every film tier, and every add-on end to end — and captures the lead even when the customer doesn't finish.
            </p>

            <ul class="mt-7 space-y-3">
              {caseBullets.map((b) => (
                <li class="flex items-start gap-3 text-[1rem] text-text leading-snug">
                  <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                  </span>
                  <span>{b}</span>
                </li>
              ))}
            </ul>

            <p class="mt-7 pt-6 border-t border-line text-text-soft text-[0.98rem] italic leading-relaxed">
              The same engine runs auto, residential, and commercial — priced and routed the way each one actually closes.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- PEER TESTIMONIAL — placed adjacent to a primary CTA, per checklist §8. -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-4xl px-5 sm:px-8 py-20 sm:py-24">
        <figure class="card p-7 sm:p-10">
          <div class="flex items-center gap-1 text-accent" aria-label="5 out of 5 stars">
            {Array.from({ length: 5 }).map(() => (
              <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <polygon points="12 2 15 9 22 9.5 17 14.5 18.5 22 12 18 5.5 22 7 14.5 2 9.5 9 9"/>
              </svg>
            ))}
          </div>
          <blockquote class="mt-5 text-[1.15rem] sm:text-[1.3rem] text-text leading-relaxed font-display">
            "Bill made two websites for me over the past few years. I had a great experience working with Bill from start to finish. They took the time to really understand my business, my goals, and the kind of online presence I was trying to build. Communication was clear and consistent throughout the entire process, which made everything feel smooth and stress-free."
          </blockquote>
          <figcaption class="mt-6 flex items-center gap-3">
            <div class="font-display font-semibold text-text">Eric Fitch</div>
            <span class="text-text-muted text-[0.95rem]">Owner, Fitchin Auto Detail &amp; Window Tinting · Coshocton, OH</span>
          </figcaption>
        </figure>

        <!-- Inline CTA adjacent to the testimonial. -->
        <div class="mt-10 text-center">
          <p class="text-text-soft text-[0.97rem] mb-5">
            Want the same tint quote system priced around your exact menu?
          </p>
          <a href={QUOTE_HREF} class="btn-primary">
            Get My Website Estimate
            <span class="text-[0.85em] font-normal opacity-80">→ 60 sec</span>
          </a>
          <p class="mt-3 text-[0.85rem] text-text-muted">
            ~60 seconds. No spam, no pushy sales call.
          </p>
        </div>
      </div>
    </section>

    <!-- SIGNATURE FEATURES -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Signature Features</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            Three things a real tint wizard does that a contact form can’t.
          </h2>
        </div>

        <div class="mt-14 grid md:grid-cols-3 gap-8 lg:gap-10">
          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M5 17h14M5 17a2 2 0 0 1-2-2v-2a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v2a2 2 0 0 1-2 2M5 11l1.6-4.8A2 2 0 0 1 8.5 5h7a2 2 0 0 1 1.9 1.2L19 11"/><circle cx="7.5" cy="17" r="1.5"/><circle cx="16.5" cy="17" r="1.5"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">{signatureFeatures[0].title}</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">{signatureFeatures[0].body}</p>
          </div>

          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">{signatureFeatures[1].title}</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">{signatureFeatures[1].body}</p>
          </div>

          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">{signatureFeatures[2].title}</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">{signatureFeatures[2].body}</p>
          </div>
        </div>
      </div>
    </section>

    <!-- PROOF -->
    <section class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Real Screens</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            Real screens from a live tint quote system — not a mockup.
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            From the live Fitchin install. The same flow runs auto, residential, and commercial.
          </p>
        </div>

        <div class="mt-12 grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {proofItems.map((p) => (
            <figure class="card overflow-hidden flex flex-col">
              <div class="relative aspect-[10/13] bg-surface-2 overflow-hidden border-b border-line">
                <img
                  src={p.src}
                  alt={p.title}
                  class="absolute inset-0 w-full h-full object-cover object-top"
                  width="1320"
                  height="2708"
                  loading="lazy"
                  decoding="async"
                />
              </div>
              <figcaption class="p-5 flex flex-col gap-2">
                <h3 class="font-display font-semibold text-[1rem] text-text">{p.title}</h3>
                <p class="text-[0.92rem] text-text-soft leading-relaxed">{p.description}</p>
              </figcaption>
            </figure>
          ))}
        </div>
      </div>
    </section>

    <!-- HOW IT WORKS -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-5xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> How It Works</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            Built to feel simple for customers — and powerful behind the scenes.
          </h2>
        </div>

        <div class="mt-10 grid sm:grid-cols-2 gap-5">
          {howItWorks.map((t) => (
            <div class="card p-6">
              <div class="font-display text-[0.74rem] uppercase tracking-[0.18em] text-accent mb-2">{t.label}</div>
              <div class="text-[1rem] text-text leading-relaxed">{t.value}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- WHO THIS IS FOR -->
    <section class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-4xl px-5 sm:px-8 py-20 sm:py-24">
        <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Fit Check</span>
        <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
          This is built for tint shops ready to stop quoting over the phone.
        </h2>
        <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
          This is a good fit if:
        </p>

        <ul class="mt-7 space-y-3">
          {whoFor.map((b) => (
            <li class="flex items-start gap-3 text-[1rem] text-text leading-snug">
              <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>{b}</span>
            </li>
          ))}
        </ul>

        <p class="mt-8 text-text-muted text-[0.95rem] italic">
          This isn't for shops that want the cheapest one-page template or a contact form bolted onto a stock theme.
        </p>
      </div>
    </section>

    <!-- OWNERSHIP / RISK REVERSAL — strongest objection-killer; #ownership is a Google Ads sitelink target. -->
    <section id="ownership" class="relative border-b border-line">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Ownership</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            Your site. Your code. Your hosting. <span class="text-accent">No rent.</span>
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            Unlike Wix, Squarespace, or any rented site builder, the whole stack is yours at handoff — the code, the domain, the hosting account. No required monthly. Cancel anything you're paying us anytime, and the site keeps running.
          </p>
        </div>

        <div class="mt-14 grid md:grid-cols-3 gap-8 lg:gap-10">
          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="15" r="4"/><line x1="10.85" y1="12.15" x2="19" y2="4"/><line x1="18" y1="5" x2="20" y2="7"/><line x1="15" y1="8" x2="17" y2="10"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">You own the code</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">
              At handoff the repo and domain are in your name. No required monthly. The site keeps running on its own — nobody can yank it down, lock you out, or hold a redesign over your head.
            </p>
          </div>

          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/><line x1="1" y1="1" x2="23" y2="23"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">No more builder rent</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">
              Stop paying Wix or Squarespace every month for a locked-down template you'll never own. Yours is custom-built around how your tint shop quotes and books — not a theme you fight with.
            </p>
          </div>

          <div class="flex flex-col gap-4">
            <span class="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-accent/15 text-accent" aria-hidden="true">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M17.5 19a4.5 4.5 0 1 0-1.5-8.75 5 5 0 0 0-9.65 1.4A4 4 0 0 0 6 19h11.5z"/></svg>
            </span>
            <h3 class="font-display font-semibold text-[1.15rem] text-text">$0 hosting bill</h3>
            <p class="text-[0.95rem] text-text-soft leading-relaxed">
              The site runs on hosting that's free for shops your size — no server bill from us required, and none if you ever walk away. You're not locked into a hosting contract.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- OFFER + PRICING — two product lines, six tiers, install price only.
         Public pricing here pulls from PRICING.md → PUBLIC. Care plans
         ($97 / $197 / $297) are INTERNAL only — sold on the call, never
         rendered on the page. See PRICING.md → INTERNAL section. -->
    <section id="pricing" class="relative border-b border-line bg-surface-1/30">
      <div class="mx-auto max-w-6xl px-5 sm:px-8 py-20 sm:py-24">
        <div class="max-w-3xl">
          <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Pricing</span>
          <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
            Two product lines. <span class="text-accent">You own everything.</span>
          </h2>
          <p class="mt-5 text-text-soft text-[1.05rem] leading-relaxed">
            Already have a site you're keeping? Add just the wizard — <strong class="text-text">Form line, starts at $997</strong>. Need a new site too? Build the system in from day one — <strong class="text-text">Website line, starts at $1,497</strong>. No required monthly on any tier. You own your code, your site, your domain — outright.
          </p>
        </div>

        <!-- FORM LINE — for shops keeping their existing site. -->
        <div class="mt-14">
          <div class="flex flex-wrap items-baseline gap-3 mb-6">
            <h3 class="font-display font-bold text-[1.5rem] text-text">Form / Wizard line</h3>
            <span class="text-text-muted text-[0.95rem]">Add the wizard to your existing site</span>
          </div>

          <div class="grid md:grid-cols-3 gap-5">
            {formLineTiers.map((t) => (
              <div class={'card p-7 flex flex-col ' + (t.highlight ? 'border-accent/60 bg-accent/[0.05] relative' : '')}>
                {t.highlight && (
                  <span class="absolute -top-3 left-7 inline-flex items-center rounded-full bg-accent px-2.5 py-1 font-display text-[0.62rem] font-bold uppercase tracking-[0.16em] text-bg">
                    {t.tag}
                  </span>
                )}
                <div class="font-display text-[0.7rem] uppercase tracking-[0.18em] text-text-muted">{t.code}</div>
                <h4 class="mt-1 font-display font-bold text-[1.2rem] text-text">{t.name}</h4>
                <div class="mt-4 font-display">
                  <div class="text-[2rem] leading-none font-bold text-text">{t.setup}</div>
                  <div class="mt-1 text-[0.85rem] text-text-muted uppercase tracking-[0.14em]">install · one-time</div>
                </div>
                <p class="mt-3 text-[0.85rem] text-text-soft leading-snug">
                  You own everything. No required monthly.
                </p>
                <p class="mt-5 text-[0.95rem] text-text-soft leading-relaxed">{t.blurb}</p>
                <ul class="mt-5 space-y-2 flex-1">
                  {t.features.map((f) => (
                    <li class="flex items-start gap-2 text-[0.9rem] text-text leading-snug">
                      <span class="mt-1 inline-flex h-4 w-4 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                        <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                      </span>
                      <span>{f}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        <!-- WEBSITE LINE — for shops building new. -->
        <div class="mt-16">
          <div class="flex flex-wrap items-baseline gap-3 mb-6">
            <h3 class="font-display font-bold text-[1.5rem] text-text">Website line</h3>
            <span class="text-text-muted text-[0.95rem]">Full site + the system, built together</span>
          </div>

          <div class="grid md:grid-cols-3 gap-5">
            {websiteLineTiers.map((t) => (
              <div class={'card p-7 flex flex-col ' + (t.highlight ? 'border-accent/60 bg-accent/[0.05] relative' : '')}>
                {t.highlight && (
                  <span class="absolute -top-3 left-7 inline-flex items-center rounded-full bg-accent px-2.5 py-1 font-display text-[0.62rem] font-bold uppercase tracking-[0.16em] text-bg">
                    {t.tag}
                  </span>
                )}
                <div class="font-display text-[0.7rem] uppercase tracking-[0.18em] text-text-muted">{t.code}</div>
                <h4 class="mt-1 font-display font-bold text-[1.2rem] text-text">{t.name}</h4>
                <div class="mt-4 font-display">
                  <div class="text-[2rem] leading-none font-bold text-text">{t.setup}</div>
                  <div class="mt-1 text-[0.85rem] text-text-muted uppercase tracking-[0.14em]">install · one-time</div>
                </div>
                <p class="mt-3 text-[0.85rem] text-text-soft leading-snug">
                  You own everything. No required monthly.
                </p>
                <p class="mt-5 text-[0.95rem] text-text-soft leading-relaxed">{t.blurb}</p>
                <ul class="mt-5 space-y-2 flex-1">
                  {t.features.map((f) => (
                    <li class="flex items-start gap-2 text-[0.9rem] text-text leading-snug">
                      <span class="mt-1 inline-flex h-4 w-4 items-center justify-center rounded-full bg-accent/15 text-accent flex-shrink-0" aria-hidden="true">
                        <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                      </span>
                      <span>{f}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        <!-- TIMELINE + UPGRADE PATH. -->
        <div class="mt-14 grid sm:grid-cols-2 gap-5">
          <div class="card p-6">
            <h4 class="font-display font-semibold text-[1rem] text-text mb-2">Timeline</h4>
            <p class="text-[0.94rem] text-text-soft leading-relaxed">
              3–5 weeks from kickoff to launch on Website tiers; 1–3 weeks on Form line. Code is yours at handoff. No long-term contract.
            </p>
          </div>
          <div class="card p-6">
            <h4 class="font-display font-semibold text-[1rem] text-text mb-2">Upgrade path: Form → Website</h4>
            <p class="text-[0.94rem] text-text-soft leading-relaxed">
              Start on Form, upgrade later. Your original install credits against the new Website tier — Form T2 ($1,497) → Website T2 ($2,497) costs the $1,000 difference. The wizard + dashboard you already own carry over.
            </p>
          </div>
        </div>

        <p class="mt-7 text-[0.86rem] text-text-muted italic max-w-3xl leading-relaxed">
          Ballpark, not a contract. Bill prices the final build around your exact segments, film tiers, vehicle logic, and add-ons before you commit.
        </p>

        <div class="mt-10 flex justify-center">
          <a href={QUOTE_HREF} class="btn-primary">
            Get My Website Estimate
            <span class="text-[0.85em] font-normal opacity-80">→ 60 sec</span>
          </a>
        </div>
      </div>
    </section>

    <QuoteFlowSection />

    <!-- FAQ -->
    <section class="relative border-b border-line">
      <div class="mx-auto max-w-3xl px-5 sm:px-8 py-20 sm:py-24">
        <span class="eyebrow"><span class="dot" aria-hidden="true"></span> FAQ</span>
        <h2 class="mt-5 text-[1.9rem] sm:text-[2.4rem] leading-[1.07] font-bold">
          Honest answers, no fine print.
        </h2>

        <div class="mt-10 space-y-3">
          {faqs.map((f, i) => (
            <details class="card p-6 group" open={i === 0}>
              <summary class="cursor-pointer list-none font-display font-semibold text-[1rem] sm:text-[1.05rem] text-text flex items-start justify-between gap-4">
                <span>{f.q}</span>
                <span class="text-accent text-[1.2rem] leading-none mt-0.5 group-open:rotate-45 transition-transform" aria-hidden="true">+</span>
              </summary>
              <p class="mt-4 text-[0.97rem] text-text-soft leading-relaxed">{f.a}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- FINAL CTA -->
    <section id="apply" class="relative overflow-hidden">
      <div class="grid-bg absolute inset-0 z-0 opacity-40" aria-hidden="true"></div>
      <div class="accent-glow absolute -bottom-40 -right-40 w-[640px] h-[640px] z-0" aria-hidden="true"></div>

      <div class="relative z-10 mx-auto max-w-4xl px-5 sm:px-8 py-24 sm:py-32 text-center">
        <h2 class="text-[2.1rem] sm:text-[2.9rem] leading-[1.05] font-bold tracking-tight">
          Most websites stop at the click. <span class="text-accent">We build the path after it.</span>
        </h2>
        <p class="mt-6 text-text-soft text-[1.05rem] sm:text-[1.15rem] leading-relaxed max-w-2xl mx-auto">
          Give your shop a website that prices auto tint on the spot, books home and commercial estimates, and captures every lead in between.
        </p>

        <div class="mt-10 flex flex-wrap justify-center gap-3">
          <a href={QUOTE_HREF} class="btn-primary">
            Get My Website Estimate
            <span class="text-[0.85em] font-normal opacity-80">→ 60 sec</span>
          </a>
          <a href={PHONE_TEL} class="btn-ghost">
            Call/Text Bill
            <span aria-hidden="true">→</span>
          </a>
        </div>

        <p class="mt-6 text-[0.92rem] text-text-soft">
          ~60 seconds. No spam, no list-selling, no pushy sales call.
        </p>
        <p class="mt-2 text-[0.86rem] text-text-muted">
          Or call/text directly:
          <a href={PHONE_TEL} class="text-accent font-medium underline decoration-accent/40 underline-offset-4 hover:decoration-accent transition-colors whitespace-nowrap">{PHONE_DISPLAY}</a>
        </p>
      </div>
    </section>
    </div>
  </main>

  <!-- MINIMAL FOOTER — no service nav. Copyright + contact + required legal only. -->
  <footer class="relative z-10 border-t border-line bg-bg">
    <div class="mx-auto max-w-7xl px-5 sm:px-8 py-8 flex flex-col sm:flex-row items-center justify-between gap-4 text-[0.85rem] text-text-muted">
      <div>© 2026 Designed to Elevate. All rights reserved.</div>
      <div class="flex flex-wrap gap-4 justify-center items-center">
        <a href={PHONE_TEL} class="hover:text-accent transition-colors">{PHONE_DISPLAY}</a>
        <a href={`mailto:${EMAIL}`} class="hover:text-accent transition-colors">{EMAIL}</a>
        <a href="/privacy" class="hover:text-accent transition-colors">Privacy</a>
        <a href="/terms" class="hover:text-accent transition-colors">Terms</a>
      </div>
    </div>
  </footer>

  <!-- STICKY MOBILE CTA — controlled by LandingLayout's IntersectionObserver
       which watches [data-hero-cta] and #apply. -->
  <div
    data-sticky-cta
    class="md:hidden fixed inset-x-0 bottom-0 z-40 border-t border-line bg-bg/95 backdrop-blur supports-[backdrop-filter]:bg-bg/85 px-3 py-3 pb-[max(env(safe-area-inset-bottom),0.75rem)] translate-y-full opacity-0 pointer-events-none transition-[transform,opacity] duration-300 ease-out"
    aria-label="Window tint website conversion actions"
  >
    <div class="flex gap-2">
      <a href={PHONE_TEL} class="btn-ghost flex-1 !px-2 !text-[0.85rem]" aria-label={`Call or text Bill at ${PHONE_DISPLAY}`}>
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92Z"/></svg>
        Call/Text
      </a>
      <a href={QUOTE_HREF} class="btn-primary flex-1 !px-2 !text-[0.85rem]">
        Get My Website Estimate
        <span aria-hidden="true">↗</span>
      </a>
    </div>
  </div>

</LandingLayout>
